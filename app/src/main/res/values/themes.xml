<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PasswordStorage" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/gray_2</item>
        <item name="android:navigationBarColor">@color/gray_2</item>
    </style>

    <style name="Theme.PasswordStorage" parent="Base.Theme.PasswordStorage" />

    <style name="CalcButtonStyle">
        <item name="android:backgroundTint">@color/purple</item>
        <item name="android:textColor">@color/white_2</item>
        <item name="android:textSize">34sp</item>
        <item name="android:padding">0dp</item>
        <item name="iconPadding">0dp</item>
        <item name="cornerRadius">0dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_weight">1</item>

    </style>
    <style name="CalcLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">3dp</item>
        <item name="android:background">@color/purple</item>
    </style>

    <style name="NavMenu"> </style>

    <style name="MaterialButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="backgroundTint">@null</item>
    </style>
</resources>